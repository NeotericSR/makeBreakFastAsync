# dotnet new console -> start a project
# dotnet restore -> resolve build assets by accessing required .NET Core packages
# dotnet run 

# To debug use omnisharp loaded in vs code then debugging icon side menu

